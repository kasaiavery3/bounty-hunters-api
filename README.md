# Bounty Hunters API
API using Express and Mongoose

```zsh
touch app.js
```

```zsh
npm init -y
```

```zsh
npm install express mongoose
```

```json
{
    "scripts": {
        "start": "node app.js",
        "test": "mocha",
        "nodemon": "nodemon"
    }
}
```
